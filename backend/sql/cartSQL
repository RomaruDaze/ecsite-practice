-- 1. Delete the table if it already exists
DROP TABLE IF EXISTS cart;

-- 2. Create the table with the new description column
CREATE TABLE cart (
    id SERIAL PRIMARY KEY,
    user_id INT NOT NULL,
    item_id INT NOT NULL
);

-- 3. Insert the data with ~100-word descriptions
INSERT INTO cart (user_id,item_id) VALUES
(
    1,1
),
(
    1,2
),
(
    1,3
),
(
    1,4
),
(
    1,5
)
